model = "gpt-5.3-codex"
model_reasoning_effort = "xhigh"
suppress_unstable_features_warning = true
web_search = "live"
personality = "pragmatic"

[agents]
max_threads = 12
max_depth = 3

[agents.coder]
description = "Author code changes with TK doctrine and explicit proof signals."
config_file = "~/.codex/agents/coder.toml"

[agents.reducer]
description = "Reduce incidental complexity while preserving coder contract and invariants."
config_file = "~/.codex/agents/reducer.toml"

[agents.fixer]
description = "Own safety/regression review and final authority for safety conflicts."
config_file = "~/.codex/agents/fixer.toml"

[agents.mentor]
description = "Score doctrine-fit and provide actionable improvement guidance."
config_file = "~/.codex/agents/mentor.toml"

[agents.selector]
description = "Drive `$select` plan-to-task decomposition for maximum safe parallelism."
config_file = "~/.codex/agents/selector.toml"

[agents.joiner]
description = "Route patch artifacts to the right PRs and coordinate join-style integration."
config_file = "~/.codex/agents/joiner.toml"

[agents.integrator]
description = "Own delivery packaging after fixer acceptance (`$commit`/`$patch`) with artifact hygiene."
config_file = "~/.codex/agents/integrator.toml"

[features]
unified_exec = true
steer = true
collaboration_modes = true
apps = true
personality = true
memories = true
sqlite = true
responses_websockets_v2 = true
multi_agent = true
prevent_idle_sleep = true
voice_transcription = true

[tui]
experimental_mode = "execute"
status_line = ["current-dir", "git-branch", "context-remaining", "model-with-reasoning"]
theme = "coldark-dark"
