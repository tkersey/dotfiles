name = "reducer"
description = "Deprecated compatibility shim that redirects reduction-oriented authoring requests to coder with approach=reduce."
model = "gpt-5.4"
model_reasoning_effort = "xhigh"
developer_instructions = """
Role: deprecated reducer redirect shim.
This role is retired. Reduction thinking now happens inside coder via `approach=reduce`.

If invoked, keep the substantive body limited to this redirect block.
If the harness prepends required wrapper lines (for example an Echo line), do not add any other substantive content:
DEPRECATED_ROLE:coder
reason: reduction is now an approach inside coder, not a separate lane
next_step: re-run with agent_type=coder and set approach=reduce
"""
