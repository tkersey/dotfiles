# `$resolve` 8.0 Patch Notes

The observation language now closes before implementation.

```text
AC-v2
-> RB-v1
-> CEX-v1
-> CEB-v2
-> MBK/RC
-> realization
-> targeted conformance
-> PHI-v1
-> terminal holdout
```

The former governor fuse remains a diagnostic backstop. Semantic admission, novelty, batch, and potential gates now own mutation authority.

Installing this skill before the new `resolve-c3` capabilities intentionally makes material work analysis-only.
