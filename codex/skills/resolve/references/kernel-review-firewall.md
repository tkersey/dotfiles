# Kernel Review Firewall

Kernel review may change accepted behavior.

Conformance review may not.

A conformance finding is:

- implementation violates an accepted law;
- proof is missing or stale;
- code surface has no kernel mapping;
- realization diverges from the accepted kernel.

A finding that adds behavior crosses back to kernel/spec review.
