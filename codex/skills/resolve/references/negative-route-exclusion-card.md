# Negative Route Exclusion Card

Use `negative_route_exclusion_card` / `NREC-v1` when a selected route is falsified or an active exclusion should shape routing.

A selected normal form may not violate an active NREC unless the NREC is reopened, stale, superseded, or explicitly accepted as risk.
