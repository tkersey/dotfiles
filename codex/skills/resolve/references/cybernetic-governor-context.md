# Cybernetic Governor Context

Use `$cybernetic` when repeated review events imply system structure.

```yaml
cybernetic_context:
  required:
  system_type:
  pattern:
  feedback_loop:
  leverage_level:
  selected_intervention:
    route:
    downstream_skill:
  local_patch_allowed:
  monitoring_or_probe:
```

If same-cluster recurrence occurs, either emit this context or state why the cluster is not a system pattern.
