# Proof Matrix Gate

After same-cluster recurrence, tests must cover the family.

```yaml
proof_matrix_gate:
  required:
  one_test_per_wound:
  family_matrix_present:
  duplicate_fixture_cases:
  tests_retired_or_merged: []
  allowed:
  reason:
```

Block or justify tests that only encode the latest wound.
