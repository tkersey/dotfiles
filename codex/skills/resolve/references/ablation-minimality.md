# Ablation Minimality

Every surviving edit atom needs a witness showing that removal breaks:

- a branch-liable counterexample;
- original acceptance;
- compatibility within scope;
- current proof.

`one_minimal: yes` means no single surviving edit atom can be removed while retaining the required proof set.

It is local minimality, not proof of global program minimality.
