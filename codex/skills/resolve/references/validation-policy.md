# Validation Policy

Review cleanliness is not validation.

After three clean reviews, run the full project validation suite. If validation fails, route contested actionability through `$review-adjudication`, invoke `$review-compression-compiler` when clustered or surface-bearing, then `$fixed-point-driver`.

If no validation exists, do not treat validation as passed. Block or require explicit manual-only proof acceptance.
