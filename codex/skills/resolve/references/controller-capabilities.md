# Controller Capabilities

Material kernel-mode resolve requires:

```text
campaign_base_v1
minimum_behavioral_kernel_v1
mbkc_v1
kernel_quotient_v1
semantic_surface_v1
proof_compression_v1
physical_apply
physical_commit
physical_push
closure_horizon_v1
```

If missing, fail closed for delivery mutation.

Use `controller_preflight.py`.
