# RGR Gate

Validate record files with:

```bash
python codex/skills/resolve/tools/rgr_gate.py <review-governor-record.yml>
```

Gate pass is not correctness proof. Gate failure blocks mutation or closure.
