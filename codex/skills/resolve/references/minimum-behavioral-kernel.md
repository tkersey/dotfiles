# Minimum Behavioral Kernel

The kernel is the smallest behavioral model that preserves:

- original acceptance;
- accepted compatibility;
- branch-liable counterexamples;
- explicit non-goals.

It names authorities, observations, equivalence classes, operations, transitions, laws, and forbidden states.

It must not encode distinctions that no accepted observation can detect.
