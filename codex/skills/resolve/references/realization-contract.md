# Kernel Realization Contract

A realization must:

- start from campaign base;
- implement only accepted kernel elements;
- map every code construct to a kernel element;
- retire superseded surfaces;
- stay within the semantic-surface ceiling;
- return to kernel review on a new observation.

A realization is replaceable. Do not patch it after the kernel changes.
