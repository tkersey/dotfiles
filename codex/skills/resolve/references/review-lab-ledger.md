# Review Lab Ledger

The lab may patch, test, and fail routes.

The lab is disposable. It exists to learn.

Lab route failures should create negative-ledger captures.

Do not cherry-pick lab commits into delivery by default.
