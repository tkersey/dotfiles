# PR Sweep Contract

After each push, inspect the associated PR.

Prefer complete paginated review-thread inventory with total count, page info, thread IDs, resolved/outdated state, path/line, latest comment, and commit/head when available.

Every in-scope PR item uses the same adjudication -> route receipt -> compiler when triggered -> fixed-point flow.
