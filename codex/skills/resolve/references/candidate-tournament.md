# Candidate Tournament

Default route classes:

```text
no-change/proof-only
subtractive
existing-owner
representation
boundary/protocol
local baseline
```

Candidates share the same immutable base, basis, acceptance contract, and verifier.

A candidate may lose because it is incorrect, reuses a negative route, exceeds scope, or has a dominated semantic cost.
