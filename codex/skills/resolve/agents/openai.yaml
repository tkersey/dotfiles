policy:
  allow_implicit_invocation: true
interface:
  display_name: "Resolve"
  short_description: "CAS-first branch resolution with abstraction route selection"
  default_prompt: "Use $resolve for this task. Follow CAS-first review, full adjudication routes, the Review-Closure Abstraction Ladder, surface-budgeted fixes, validation, push, and PR sweep."
