policy:
  allow_implicit_invocation: true
interface:
  display_name: "Resolve"
  short_description: "Compile review observations into a quotiented behavioral kernel, realize one normal form, prove recomposition, and close the branch."
  default_prompt: "Use $resolve. Build MBK-v1 and RC-v1 before mutation: factor obligations, quotient indistinguishable distinctions with congruence proof, ablate discharged surfaces, normalize owners, realize one design with $fixed-point-driver, audit recomposition, then prove/push/close."
