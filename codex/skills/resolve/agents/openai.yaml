policy:
  allow_implicit_invocation: true
interface:
  display_name: "Resolve"
  short_description: "CAS-first Review Governor"
  default_prompt: "Use $resolve as a Review Governor: reviews sense, the governor decides, implementation obeys. Emit review_governor_record, govern same-cluster recurrence, measure decision impact and material improvement."
