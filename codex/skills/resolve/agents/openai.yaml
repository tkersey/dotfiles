policy:
  allow_implicit_invocation: true
interface:
  display_name: "Resolve"
  short_description: "CAS-first branch resolution with distillation and negative ledger"
  default_prompt: "Use $resolve for this task. Follow CAS-first review, full adjudication, route-wave artifacts, RCP/RDP gates, negative-ledger route memory, universalist_check falsification, review distillation when required, slice commits, fixed-point handoff, validation, push, and PR sweep."
