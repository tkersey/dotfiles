policy:
  allow_implicit_invocation: true
interface:
  display_name: "Resolve"
  short_description: "Counterexample Compression Compiler"
  default_prompt: "Use $resolve as C³: freeze delivery, compress review findings into a counterexample basis, compare independent candidate routes by semantic cost, ablate the winner, and allow delivery only through a current MRPC-v1."
