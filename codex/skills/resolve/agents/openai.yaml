policy:
  allow_implicit_invocation: true
interface:
  display_name: "Resolve"
  short_description: "CAS-first branch resolution with required RCP packets"
  default_prompt: "Use $resolve for this task. Follow CAS-first review, full adjudication, required review_compression_packet gates, slice commits, fixed-point handoff, validation, push, and PR sweep."
