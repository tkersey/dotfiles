policy:
  allow_implicit_invocation: true
interface:
  display_name: "Resolve"
  short_description: "Cleanroom Review Compiler"
  default_prompt: "Use $resolve as a cleanroom Review Compiler: freeze delivery after finding-bearing review, learn in lab, compile counterexample contract and delivery recipe, ablate, then rederive the minimal delivery patch."
