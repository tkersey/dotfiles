policy:
  allow_implicit_invocation: true
interface:
  display_name: "Resolve"
  short_description: "Review Governor with mutation permit interlock"
  default_prompt: "Use $resolve as a Review Governor: reviews sense, the governor decides, implementation obeys. After same-cluster recurrence, emit RGR-V2-MUTATION-PERMIT before mutation."
