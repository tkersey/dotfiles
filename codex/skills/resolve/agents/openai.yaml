policy:
  allow_implicit_invocation: true
interface:
  display_name: "Resolve"
  short_description: "CAS-first branch resolution with route-wave artifacts"
  default_prompt: "Use $resolve for this task. Follow CAS-first review, full adjudication, route-wave artifacts, required review_compression_packet gates, universalist_check falsification, slice commits, fixed-point handoff, validation, push, and PR sweep."
