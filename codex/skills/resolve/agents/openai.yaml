policy:
  allow_implicit_invocation: true
interface:
  display_name: "Resolve"
  short_description: "CAS-first branch resolution to pushed readiness"
  default_prompt: "Use $resolve for this task. Follow CAS-first review, full adjudication routes, surface-budgeted fixes, validation, push, and PR sweep."
