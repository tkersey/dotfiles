policy:
  allow_implicit_invocation: false
interface:
  display_name: "Resolve"
  short_description: "Resolve branches with a CAS-first review loop and native fallback only after recorded CAS failure."
  default_prompt: "Use $resolve for this task. Follow CAS-first backend selection; do not run native Codex review unless the skill's fallback condition is recorded."
