interface:
  display_name: "Ledger"
  short_description: "Operate the passive artifact runtime"
  default_prompt: "Use $ledger to bootstrap Ledger 1.0.3 or newer within major version 1 once, operate or debug an owner-selected passive definition through the native CLI, explain the exact definition-relative proof and custody boundary, author the smallest bounded owner-local definition when needed, or perform exact transaction-bound recovery without acquiring semantic or workflow authority."
policy:
  allow_implicit_invocation: true
