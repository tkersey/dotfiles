interface:
  display_name: "Ledger"
  short_description: "Validate kernels and coordinate Ledger lifecycle"
  default_prompt: "Use $ledger to bootstrap the native CLI once, validate or derive Actuating Artifact Kernel contracts without taking semantic authority, coordinate post-closure source-memory evaluation, address Universalist plans and receipts, or route other native Ledger source work."
policy:
  allow_implicit_invocation: true
