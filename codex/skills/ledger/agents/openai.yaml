interface:
  display_name: "Ledger"
  short_description: "Bootstrap and coordinate Ledger CLI access"
  default_prompt: "Use $ledger to ensure ledger is available on PATH, then inspect repo-local Ledger sources through their native doctor, path, and query commands."
policy:
  allow_implicit_invocation: true
