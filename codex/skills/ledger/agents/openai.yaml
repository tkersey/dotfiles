interface:
  display_name: "Ledger"
  short_description: "Bootstrap and coordinate Ledger CLI access"
  default_prompt: "Use $ledger to ensure ledger is available on PATH, then inspect the repo-local Ledger stores."
policy:
  allow_implicit_invocation: true
