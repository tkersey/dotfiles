# Composition Guidance

Use `$logophile` as a language-surface and doctrine-compiler pass, not as an operational replacement.

## After review adjudication

Use `$logophile` to sharpen:

- reviewer replies;
- rebuttals;
- acknowledgements;
- disposition summaries;
- resolve-selection explanations;
- proof-only or no-change wording.

Preserve raw comment identity, disposition, evidence basis, criteria, and overturn condition. Do not turn adjudication into implementation.

## After fixed-point-driver

Use `$logophile` to sharpen:

- Route Receipts;
- Fixed-Point Bottom Lines;
- closure notes;
- PR-facing summaries;
- evidence-backed blocked/ready wording.

Preserve gate status, proof receipts, and unresolved blockers.

## After ablation workflows

Use `$logophile` to make deletion/collapse/canonicalization language precise:

- `ablation: not-required` must name evidence.
- keep/delete/collapse/canonicalize decisions must not sound like taste.
- Ablative Isomorphism Cards must state the preservation relation.
- Avoid using `isomorphic` when the relation is really `refinement-preserving`.

## After $actuating

Use `$logophile` to sharpen the Actuation Bottom Line:

- lever;
- action;
- proof of movement;
- next bottleneck.

Do not soften an executable next step into a vague recommendation.

## After $seq or $learnings

Use `$logophile` to make forensic/cartographic/precedential outputs readable without losing provenance:

- preserve citations, source paths, timestamps, and freshness;
- preserve evidence strength;
- distinguish observation, claim, memory, summary, speculation, and precedent;
- preserve analogy, distinguishing facts, supersession status, and action delta;
- keep the Knowledge or Precedent Bottom Line copy-pasteable.

Do not turn remembered context into governing precedent merely by polishing it.

## After simulation or world-modeling workflows

Use `$logophile` to sharpen:

- real-system and simulated boundaries;
- state, action, transition, and observation vocabulary;
- fidelity targets and unsupported zones;
- counterfactual interventions and held-constant assumptions.

Do not polish an approximation into a prediction. Preserve uncertainty and the fidelity boundary.

## After evaluation, grading, or adjudication workflows

Use `$logophile` to sharpen:

- criteria;
- evidence and counterevidence;
- materiality;
- disposition;
- confidence;
- dispositive factor;
- what would change the ruling.

Keep the operating mode (`ADJUDICATIVE`) separate from an explicitly requested persona (`Arbiter`).

## Naming skills, modes, and personas

Prefer names that encode the job or success condition:

- `fixed-point-driver` names the convergence condition.
- `ablation_auditor` names the surface-removal audit.
- `actuating` names state-changing plan execution.
- `logophile` names the language craft layer.

Keep grammar aligned:

- mode: `NOMOTHETIC`; persona: `Nomothete`.
- mode: `ADJUDICATIVE`; persona: `Arbiter`.
- mode: `EMULATIVE`; persona only if a role noun adds value.

Avoid names that only describe plumbing: `manager`, `coordinator`, `util`, `helper`, `processor`.

## Doctrine stacks

When another skill asks for a doctrine stack, return:

1. recommended words;
2. operator roles;
3. cash-out artifacts;
4. optional persona and core command when requested;
5. final copy-paste block.

Do not execute the operational workflow unless explicitly asked.