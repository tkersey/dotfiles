policy:
  allow_implicit_invocation: true
interface:
  display_name: "Logophile"
  short_description: "Precision wording, naming, and doctrine compiler"
  default_prompt: "Use $logophile for wording, naming, doctrine-word selection, or human-facing language surfaces."
