policy:
  allow_implicit_invocation: true
interface:
  display_name: "Logophile"
  short_description: "Precision wording, naming, doctrine, and activation phrases"
  default_prompt: "Use $logophile for wording, naming, doctrine-word selection, terse activation-phrase selection, or human-facing language surfaces. For activation phrases, optimize for behavioral leverage per token and keep the runtime phrase irreducibly short."
