policy:
  allow_implicit_invocation: false
interface:
  display_name: "Ship"
  short_description: "Publish or adopt exact validated public state"
  default_prompt: "Use $ship to open, update, or promote a proof-backed PR, or read-only adopt an exact already-public Actuating subject when no current PR tuple can represent it."
