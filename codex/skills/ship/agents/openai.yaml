policy:
  allow_implicit_invocation: false
interface:
  display_name: "Ship"
  short_description: "Publish validated work as a ready PR by default"
  default_prompt: "Use $ship to open/update/promote a proof-backed PR. Default to ready PR after full validation; create draft only with an explicit warrant."
