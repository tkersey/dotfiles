policy:
  allow_implicit_invocation: false
interface:
  display_name: "Goal Grind"
  short_description: "Consume one prepared actuation capability"
  default_prompt: "Use $goal-grind only for this explicitly marked legacy-actuating-v1 goal: consume one prepared legacy Zig actuation capability, return event-bound evidence, and stop without continuing the loop."
