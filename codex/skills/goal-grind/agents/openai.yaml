policy:
  allow_implicit_invocation: false
interface:
  display_name: "Goal Grind"
  short_description: "Execute one selected goal step"
  default_prompt: "Use $goal-grind to execute this one lead-selected actuation step, run its verifier, and return evidence without continuing the loop."
