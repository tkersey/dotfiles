policy:
  allow_implicit_invocation: true
interface:
  display_name: "tune"
  short_description: "Tune skills from orthogonal evidence"
  default_prompt: "Use $tune to compare a target skill's contract with declared in-flight, historical, provided, or worktree evidence, then brief $refine and apply only when explicitly requested."
