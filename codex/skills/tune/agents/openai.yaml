policy:
  allow_implicit_invocation: true
interface:
  display_name: "Tune"
  short_description: "Tune skills with goal-native decision deltas"
  default_prompt: "Use $tune to compare a target skill's contract with declared evidence, produce a skill_delta_candidate or terminal repeat state, and apply only when explicitly requested."
