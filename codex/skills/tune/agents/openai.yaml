interface:
  display_name: "tune"
  short_description: "Tune skills from observed usage evidence"
  default_prompt: "Use $tune to compare a target skill's intended contract with $seq-backed usage evidence, classify gaps, produce a $refine brief, and apply edits only when explicitly requested."
