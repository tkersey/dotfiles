# Goal-Native Tuning

`/goal` owns continuation.

`$tune` returns one bounded cycle result:

- no-action;
- skill-delta candidate;
- final brief;
- apply request;
- seq special spec handoff;
- retired finding;
- blocked.

Do not create a separate continual controller.
