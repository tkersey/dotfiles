# Matched Cohort

Matched comparison is optional observational evidence.

Match on:

```text
repo
workflow
subsystem/path
task-language signature
tool family
task size proxy
time/version window
worker/root role
```

Report balance and unmatched rows.

Never say the skill caused the measured difference.

Use matched cohorts to prioritize follow-up, not to authorize a broad edit by themselves.
