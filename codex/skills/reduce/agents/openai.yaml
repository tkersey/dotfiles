interface:
  display_name: "reduce"
  short_description: "Factor costly layers, quotient redundancy, ablate unearned surface, and normalize the survivors with a Reduction Certificate."
  brand_color: "#64748B"
  default_prompt: >
    Use $reduce. Build an evidence-backed factorization, score tax versus live value,
    check quotient congruence and essential abstractions, select winnowing operators,
    emit RC-v1, and propose reversible proof-bearing cuts. Do not implement unless asked.
policy:
  allow_implicit_invocation: true
