interface:
  display_name: "reduce"
  short_description: "Challenge costly factors without taking selection authority"
  brand_color: "#64748B"
  default_prompt: "Use $reduce to factor the candidate, challenge redundant distinctions, and prove recomposition; in Actuating composition, return one compact non-authoritative challenge and reserve RC-v1 for standalone or independently durable work."
policy:
  allow_implicit_invocation: false
