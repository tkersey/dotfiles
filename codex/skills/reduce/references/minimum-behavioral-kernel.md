# Minimum Behavioral Kernel

Use `$reduce` to propose a quotient/subtractive realization:

- merge observationally equivalent states;
- collapse duplicate authorities;
- delete local-surface validations subsumed by one law;
- retire fallback and compatibility branches;
- compact proof families.

Return a realization design, not delivery mutation.
