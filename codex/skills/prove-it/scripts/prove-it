#!/usr/bin/env bash
set -euo pipefail
cat <<'EOF'
PROVE_IT_SUBAGENT_NATIVE

Invoke `$prove-it` in a subagent-capable Codex session. The sequential host-loop driver has been removed; prove-it now runs rounds 1-9 as parallel lens_worker packets and round 10 as an oracle_worker after fan-in.

This script intentionally creates no artifacts.
EOF
exit 2
