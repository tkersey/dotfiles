# Exemplar Contract

```yaml
spec_exemplar:
  id:
  label:
  source:
  trigger:
  good_pattern:
  failure_pattern:
  reusable_rule:
  target_surface:
  measurement:
```

Keep exemplars only when they change future behavior.
