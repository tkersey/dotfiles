policy:
  allow_implicit_invocation: false
interface:
  display_name: "Spec Retro"
  short_description: "Historical learning for the spec system"
  default_prompt: "Use $spec-retro to convert multi-run spec evidence into measurable updates to $spec-pipeline."
