policy:
  allow_implicit_invocation: true
interface:
  display_name: "Make Skill"
  short_description: "Create lean observable Codex skills"
  default_prompt: "Use $ms for direct skill creation or surgery; classify the skill type, add decision instrumentation only when justified, and always validate."
