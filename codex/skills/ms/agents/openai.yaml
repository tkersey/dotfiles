policy:
  allow_implicit_invocation: false
interface:
  display_name: "ms"
  short_description: "Directly create or edit Codex skills in this repo"
  default_prompt: "Use $ms to directly create or edit a Codex skill in this repo."
