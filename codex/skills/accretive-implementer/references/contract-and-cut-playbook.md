# Contract and Cut Playbook

Before non-trivial edits:

```text
Contract:
Invariants:
Stable boundary:
Why not smaller:
Why not larger:
Proof signal:
```

A good cut is stable, owned, reviewable, and paired with a proof signal. A bad cut makes the symptom disappear while preserving the invalid producer, duplicate owner, or unwitnessed guarantee.
