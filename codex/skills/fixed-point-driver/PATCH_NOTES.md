# `$fixed-point-driver` 7.0 Patch Notes

EPG-selected actions now reuse FPS-v1/FPSR-v1 with optional policy lineage.

The driver reports actual effects and observations but never advances EPS, selects a new policy branch, or self-authorizes delivery.
