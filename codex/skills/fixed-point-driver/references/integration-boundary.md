# Integration Boundary

`$fixed-point-driver` ends at a validated worker-tree result.

It may not:

```text
apply to primary checkout
stage shared index
advance target branch
push
mark integration proof current
```

Those are `$st` workspace integration operations.
