# Drop-in upgrade manifest

This skill directory is intended to be replaced as a unit when receiving a full
skill-directory drop-in. Keep support references, agents, and templates with the
primary `SKILL.md` so directory replacement does not orphan operational contracts.
