policy:
  allow_implicit_invocation: false
interface:
  display_name: "Fixed-Point Driver"
  short_description: "Drive implementation, review, and closure to a material fixed point"
  default_prompt: "Use $fixed-point-driver for this task."
