policy:
  allow_implicit_invocation: true
interface:
  display_name: "Fixed-Point Driver"
  short_description: "Drive changesets to material fixed point"
  default_prompt: "Use $fixed-point-driver for this task."
