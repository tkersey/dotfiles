policy:
  allow_implicit_invocation: true
interface:
  display_name: "Fixed Point Driver"
  short_description: "Canonical realization and kernel conformance"
  default_prompt: "Under $resolve, compile one accepted kernel realization from campaign base, map every construct to the kernel, and return on new observations."
