# `$seq` Observability

Until the native command exists, persist structured ASL/FPS/PDAG/SDR artifacts when the workflow already retains evidence.

Future native surface:

```text
seq actuation-control-audit
```

It should measure:

```text
GCR coverage and failure class
mutation after unavailable graph control
ASL/FPS coverage
projection inversion
semantic-row recurrence
proof cadence and staleness
compaction resume versus rediscovery
subagent artifact yield
decision effects
```

Persistent `codex_internal_context` proves context continuity only. It must not independently count as a successful skill outcome.

See the package-level `SEQ_ACTUATION_CONTROL_AUDIT_SPEC.md`.
