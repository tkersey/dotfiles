policy:
  allow_implicit_invocation: false
interface:
  display_name: "Actuating"
  short_description: "Construct, orchestrate, falsify, and close"
  default_prompt: "Use $actuating to state OPERATE ARCHITECTONICALLY before architecture or abstraction selection, integrate one bounded $glaze pass with $universalist when findings make abstraction change live, and let Actuating alone adjudicate the Construction and next action."
