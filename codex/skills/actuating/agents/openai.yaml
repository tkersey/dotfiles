policy:
  allow_implicit_invocation: true
interface:
  display_name: "Actuating"
  short_description: "Plan-to-PR with explicit PR mode"
  default_prompt: "Use $actuating to convert a material plan into $st tasks, execute through fixed-point proof, validate, then ship with explicit PR mode. Ready PR is default after full validation; draft requires a warrant."
