policy:
  allow_implicit_invocation: false
interface:
  display_name: "Actuating"
  short_description: "Construct, orchestrate, falsify, and close"
  default_prompt: "Use $actuating to select a correct-by-construction architecture, orchestrate its realization and review, evaluate Counterexamples, and choose the next action while Ledger remains non-executing and Ship owns public effects."
