policy:
  allow_implicit_invocation: false
interface:
  display_name: "Actuating"
  short_description: "Govern accepted work through live evidence"
  default_prompt: "Use bare $actuating to implement, hand off to $ship, then run review-closeout; honor explicit modes, including implementation-only $actuating implement."
