policy:
  allow_implicit_invocation: true
interface:
  display_name: "Actuating"
  short_description: "Turn counterexamples into family-excluding constructions"
  default_prompt: "Use $actuating to implement the accepted Goal and compile cumulative counterexamples into law-preserving constructions. Keep implement, analyze, review-closeout, and the bare end-to-end route distinct: implement constructs and proves locally without review; analyze never mutates. Once review opens on a proved candidate, freeze it through the applicable complete evidence cut. Have Review Fold project and reclassify the corpus, adjudicate current findings, and capture accepted independent witnesses. Infer the enabling cause and choose a sibling or source-domain discriminator before implementing. Select for family exclusion, required-valid preservation, complete sanctioned-path coverage, and fewer independent semantic owners, not a preferred repair label or preservation of the incumbent. Use bounded Metanoetic challenges before Universalist when a mechanism or boundary is genuinely in question; compile the full nominated boundary contract and use Reduce to retire redundant compensation. Verify actual exact-head code, source-derived topology, claim strength, and authority for every semantic delta. Labels and agent-authored digests grant no authority. Preserve native/default Codex standard review, five auxiliary lenses, both scheduling modes, and five-standard-clean convergence. Add no Actuating gate, packet, or durable store."
