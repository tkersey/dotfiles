policy:
  allow_implicit_invocation: true
interface:
  display_name: "Actuating"
  short_description: "Turn counterexamples into family-excluding constructions"
  default_prompt: "Use $actuating to implement the accepted Goal through counterexample-driven, law-preserving construction. Follow SKILL.md for authority, admission, candidate proof, and review closure. Preserve required-valid behavior while removing the enabling cause, not just the reported example. Apply Glaze then Metanoetic verbatim in the same bounded challenger pass when the existing trigger fires; Universalist nominates at live boundaries and Reduce challenges material redundant factors. Reuse exact owner evidence. Preserve all public routes, native standard review, five auxiliaries, both scheduling modes, freezes, recovery, and five-standard-clean convergence. Add no Actuating packet, gate, or store."
