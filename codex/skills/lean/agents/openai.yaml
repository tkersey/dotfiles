interface:
  display_name: "Lean"
  short_description: "Lean 4 proving and verified programming"
  default_prompt: "Use $lean to write Lean 4 code, fix proofs, and prove correctness of implementations."
policy:
  allow_implicit_invocation: true
