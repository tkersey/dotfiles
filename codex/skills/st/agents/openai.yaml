interface:
  display_name: "st"
  short_description: "Persistent repo JSONL plan manager"
  default_prompt: "Use $st to create or update a repo JSONL plan for this task, and on first use ask whether `.step/st-plan.jsonl` should be committed or locally ignored via `.git/info/exclude`."
