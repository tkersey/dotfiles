policy:
  allow_implicit_invocation: false
interface:
  display_name: "Goal Actuating"
  short_description: "Coordinate authority-bound goal execution"
  default_prompt: "Use $goal-actuating only for this explicitly marked legacy-actuating-v1 goal: coordinate one frozen actuation-kernel operation at a time, fold current evidence, and request legacy closure without publishing."
