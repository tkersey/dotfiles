policy:
  allow_implicit_invocation: false
interface:
  display_name: "Goal Actuating"
  short_description: "Coordinate authority-bound goal execution"
  default_prompt: "Use $goal-actuating to coordinate this accepted goal through one actuation-kernel operation at a time, fold current evidence, and request live closure without publishing."
