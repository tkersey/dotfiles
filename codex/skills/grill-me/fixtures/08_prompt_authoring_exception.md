# Prompt authoring exception

User asks specifically to grill them on a prompt they are writing.

Expected:
- Prompt examples/drafts may be material only because the target is prompt authoring.
- Still prefer bounded options where possible.
