interface:
  display_name: "Parse Architecture"
  short_description: "Infer repo architecture from evidence"
  default_prompt: "Use $parse to identify this repo's dominant architecture, subsystem variants, confidence, and architecture drift."
