# C³ Control Topology

Review is a feedback system.

The old reinforcing loop was:

```text
finding -> patch -> larger reviewed surface -> finding
```

C³ changes the stock that accumulates:

```text
counterexamples accumulate
candidate code is discarded
delivery receives one minimized patch
```

Use this lens to detect when review incentives are optimizing comment closure rather than semantic compression.
