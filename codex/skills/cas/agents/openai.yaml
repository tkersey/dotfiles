interface:
  display_name: "cas"
  short_description: "Zig CAS CLI for v2 app-server checks and fanout"
  default_prompt: "Use $cas for Zig-only v2 app-server smoke checks, direct thread and turn requests, and multi-instance CAS fanout."
