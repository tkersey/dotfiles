policy:
  allow_implicit_invocation: false
interface:
  display_name: "cas"
  short_description: "Zig CAS transport for app-server and exact review attempts"
  default_prompt: "Use $cas to run, start, or wait for exact tuple-bound review attempts, and use CAS-owned passive $ledger definitions when their receipts or findings are consumed structurally; preserve opaque request identity, principal quality, verdicts, failures, and finding provenance without deciding review policy, repairs, mutation, or closure."
