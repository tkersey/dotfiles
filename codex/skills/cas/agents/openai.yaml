policy:
  allow_implicit_invocation: false
interface:
  display_name: "cas"
  short_description: "Native Codex control-plane routes and observed facts"
  default_prompt: "Use $cas to select the exact native control-plane route for app-server, account, automation, goal, review, session inquiry, smoke, instances, or conformance. Load only the selected route reference, run the required app-server preflight profile when applicable, and preserve caller-owned semantic authority."
