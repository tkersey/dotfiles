policy:
  allow_implicit_invocation: false
interface:
  display_name: "cas"
  short_description: "Zig CAS CLI for app-server checks, detached review control, fanout, and swarm conformance"
  default_prompt: "Use $cas to drive tuple-bound review attempts, preserve opaque request identity and finding provenance, and report normalized transport outcomes."
