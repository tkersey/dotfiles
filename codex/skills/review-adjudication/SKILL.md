# Retired: review-adjudication

This active skill has been retired.

Use `$review-fold` for review finding classification in goal and actuation workflows.

The retained lesson is deliberately small:

```text
claim != fact
fact != liability
liability != scope
scope != code change
code-change candidate != mutation authority
```

This file intentionally has no skill frontmatter and should not load as a skill package.
