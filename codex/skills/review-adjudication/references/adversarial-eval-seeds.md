# Adversarial eval seeds

See `../evals/adversarial-eval-seeds.yaml` for the machine-readable eval seed list.

The eval set covers false action, stale rejection, wrong-fix separation,
resolve-selection anti-laundering, warrant coverage, surface-budget consumption,
and adversarial action/parallelism coverage.
