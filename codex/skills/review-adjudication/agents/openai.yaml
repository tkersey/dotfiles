policy:
  allow_implicit_invocation: true
interface:
  display_name: "Review Adjudication"
  short_description: "Verify claims and classify C³ liability"
  default_prompt: "Verify each review claim against current code and emit branch liability plus a counterexample candidate, not a patch instruction."
