policy:
  allow_implicit_invocation: true
interface:
  display_name: "Review Adjudication"
  short_description: "Classify review liability and kernel impact"
  default_prompt: "Verify the finding and classify branch liability, kernel impact, acceptance entailment, and non-mutation disposition."
