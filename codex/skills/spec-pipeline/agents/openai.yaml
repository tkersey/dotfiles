policy:
  allow_implicit_invocation: true
interface:
  display_name: "Spec Pipeline"
  short_description: "Canonical current-spec engine"
  default_prompt: "Use $spec-pipeline. Select full, gate-only, challenge-only, lint-only, or repair mode; emit one SGR-v2 governance receipt; never emit proposed_plan."
