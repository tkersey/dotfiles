# Spec Governance Receipt

`spec_governance_receipt` / `SGR-v1` is the machine-searchable summary of whether the pipeline did its job.

It records phase presence, gate/challenge/fresh-eyes/lint impact, execution control, and retro trigger.

A complete spec handoff without SGR is not implementation-ready.
