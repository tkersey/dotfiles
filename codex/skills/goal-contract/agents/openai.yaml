policy:
  allow_implicit_invocation: true
interface:
  display_name: "Goal Contract"
  short_description: "Bind goals to source, scope, and proof"
  default_prompt: "Use $goal-contract to bind this accepted goal to its authority, artifact scope, verifier, review policy, and stop conditions."
