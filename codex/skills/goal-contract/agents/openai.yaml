policy:
  allow_implicit_invocation: true
interface:
  display_name: "Goal Contract"
  short_description: "Bind accepted truth and authority"
  default_prompt: "Use $goal-contract to compile accepted intent into goal-contract/v3 with exact outcomes, laws, authority, scope, compatibility, and acceptance without selecting architecture or granting mutation."
