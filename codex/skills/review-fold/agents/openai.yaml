policy:
  allow_implicit_invocation: true
interface:
  display_name: "Review Fold"
  short_description: "Classify and quotient review findings"
  default_prompt: "Use $review-fold to classify this current review source, quotient duplicate findings, validate RF-v2 through `$ledger run -- validate`, and emit no-code dispositions or resolution inputs without selecting repairs or granting authority."
