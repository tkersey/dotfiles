policy:
  allow_implicit_invocation: true
interface:
  display_name: "Review Fold"
  short_description: "Compile findings into counterexample classes"
  default_prompt: "Use $review-fold to classify this current review source, quotient stable law-and-boundary counterexample classes, and author a non-authorizing Counterexample Set for the current artifact-kernel construction."
