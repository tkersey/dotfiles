interface:
  display_name: "puff"
  short_description: "Launch and monitor Codex Cloud background tasks"
  default_prompt: "Use $puff doctor, then launch a Codex Cloud task in the background; for cloud PR routing loops use $puff join-operator, and optionally run $puff cas-start for advanced $cas orchestration."
