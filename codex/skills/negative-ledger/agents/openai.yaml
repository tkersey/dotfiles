policy:
  allow_implicit_invocation: true
interface:
  display_name: "Negative Ledger"
  short_description: "Map failed hypotheses into routing constraints"
  default_prompt: "Use $negative-ledger for this task."
