policy:
  allow_implicit_invocation: false
interface:
  display_name: "Negative Ledger"
  short_description: "Prune failed kernel and realization routes"
  default_prompt: "Use $negative-ledger to record and query failed semantic routes, authority models, kernel distinctions, and proof patterns."
