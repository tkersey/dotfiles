policy:
  allow_implicit_invocation: true
interface:
  display_name: "Negative Ledger"
  short_description: "Review lab route-falsification memory"
  default_prompt: "Use $negative-ledger to capture falsified lab routes and exclude failed route families from delivery recipes."
