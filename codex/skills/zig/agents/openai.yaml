interface:
  display_name: "Zig"
  short_description: "Build, lint, test, fuzz, profile, harden boundaries, and optimize Zig 0.15.2 workflows"
  default_prompt: "Use $zig to handle Zig 0.15.2 implementation, profiling, FFI, concurrency, and performance workflows. Treat the Zen of Zig as the governing first principle, then emphasize witness-driven boundaries, lint when available, differential fuzz and allocation-failure checks, dependency provenance, and deliberate profiling-stack choice: benchmark for proof, zprof for allocator and leak questions, and system or telemetry profilers for CPU or concurrency hotspots."
