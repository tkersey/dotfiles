interface:
  display_name: "Zig"
  short_description: "Build, lint, test, fuzz, harden boundaries, and optimize Zig 0.15.2 workflows"
  default_prompt: "Use $zig to handle Zig 0.15.2 implementation, FFI, concurrency, and performance workflows. Treat the Zen of Zig as the governing first principle, then emphasize witness-driven boundaries, lint when available, differential fuzz and allocation-failure checks, dependency provenance, and measured optimization with decomposition benchmarks when aggregate perf deltas hide the bottleneck."
