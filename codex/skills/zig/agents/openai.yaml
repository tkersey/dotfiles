interface:
  display_name: "Zig"
  short_description: "Build, lint, test, fuzz, and manage Zig 0.15.2 dependencies"
  default_prompt: "Use $zig to handle Zig 0.15.2 dependency workflows and validate changes with lint and correctness-first testing."
