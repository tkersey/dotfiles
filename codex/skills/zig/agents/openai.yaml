interface:
  display_name: "Zig"
  short_description: "Build, lint, test, fuzz, profile, and manage Zig 0.15.2 workflows"
  default_prompt: "Use $zig to handle Zig 0.15.2 implementation and performance workflows, including lint, correctness-first testing, and decomposition benchmarks when aggregate perf deltas span multiple abstraction layers."
