policy:
  allow_implicit_invocation: true
interface:
  display_name: "Review Compression Compiler"
  short_description: "Compress hot review clusters into governor decisions"
  default_prompt: "Use $review-compression-compiler to compress hot review findings into the review_governor_record."
