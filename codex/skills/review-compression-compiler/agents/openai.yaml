policy:
  allow_implicit_invocation: true
interface:
  display_name: "Review Compression Compiler"
  short_description: "Emit required RCP packets with universalist checks"
  default_prompt: "Use $review-compression-compiler to emit a compact review_compression_packet with universalist_check before implementation."
