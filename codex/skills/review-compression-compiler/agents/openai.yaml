policy:
  allow_implicit_invocation: true
interface:
  display_name: "Review Compression Compiler"
  short_description: "Emit RCP/RDP/NREC packets with route memory"
  default_prompt: "Use $review-compression-compiler to emit a compact RCP or RDP with negative_evidence, NREC route exclusions, universalist_check, route-wave ref, proof matrix, and fixed-point handoff before implementation."
