policy:
  allow_implicit_invocation: true
interface:
  display_name: "Review Compression Compiler"
  short_description: "Compile review findings into minimum-surface normal forms"
  default_prompt: "Use $review-compression-compiler to compress review findings into a proof-carrying normal-form handoff before implementation."
