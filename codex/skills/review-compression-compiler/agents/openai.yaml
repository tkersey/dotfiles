policy:
  allow_implicit_invocation: true
interface:
  display_name: "Review Compression Compiler"
  short_description: "Compile hot clusters into RDR normal forms"
  default_prompt: "Use $review-compression-compiler to compile the hot review cluster into the normal-form section of a resolve_decision_record."
