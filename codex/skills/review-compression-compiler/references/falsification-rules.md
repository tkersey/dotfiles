# Falsification Rules

A counterexample is novel only when existing accepted classes cannot explain its observable result under accepted operations.

Same-class recurrence invalidates the realization.

Outside-horizon evidence cannot falsify the current kernel; it challenges AC or becomes follow-up.
