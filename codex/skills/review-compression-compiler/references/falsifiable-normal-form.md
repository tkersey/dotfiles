# Falsifiable Normal Form

A normal form must state a closure prediction.

If a same-family counterexample appears after implementation:

```text
normal form = falsified
negative evidence = required
governor fuse = tripped
ordinary normal-form route = prohibited
```

The next route must change leverage level or enter distillation.
