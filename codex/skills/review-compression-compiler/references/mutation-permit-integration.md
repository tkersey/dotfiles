# Mutation Permit Integration

After same-cluster recurrence, review compression must prepare the mutation permit before implementation.

Required literal key:

```yaml
RGR-V2-MUTATION-PERMIT:
```

The permit must reject raw local mutation and must state the selected route, production embargo status, owner coarseness, boundary inventory, negative-route gate, proof-matrix gate, and handoff permission.

No permit, no mutation.
