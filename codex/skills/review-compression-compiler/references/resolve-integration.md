# `$resolve` Integration

```text
sealed AC + sealed RB + valid CEX
-> CEB/MBK/RC/RAP/PHI baseline
-> controller acceptance
-> fixed-point realization
```

The compiler never authorizes delivery mutation directly.
