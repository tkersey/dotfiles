# Minimum Hitting-Set Realization

Model candidate constructs as covering accepted laws/classes.

Require:

```text
every accepted class/law covered
every selected construct covers at least one class/law
all hard constraints satisfied
no out-of-horizon behavior realized
```

The compiler emits constraints and rankings, not code.
