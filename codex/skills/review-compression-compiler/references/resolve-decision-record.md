# Resolve Decision Record Integration

The compiler writes into `resolve_decision_record`, not a separate default packet taxonomy.

Required contribution:

- cluster owner/counterexample family;
- selected route;
- why not smaller;
- why not point fix;
- negative evidence effect;
- universalist check when needed;
- distillation decision when needed;
- proof matrix;
- handoff constraints.
