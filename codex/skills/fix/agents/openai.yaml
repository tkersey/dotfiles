interface:
  display_name: "Fix"
  short_description: "Run native codex review, repair with fixed-point-driver, validate, and repeat until review is clean"

policy:
  allow_implicit_invocation: false
