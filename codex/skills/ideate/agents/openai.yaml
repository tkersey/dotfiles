interface:
  display_name: "ideate"
  short_description: "Interrogate first, then winnow ideas into a plan seed"
  default_prompt: "Use $ideate to research the project, ask hard questions, generate many grounded ideas, winnow them, check overlap with existing work, and deliver the seed of a plan without creating tasks or implementing."
