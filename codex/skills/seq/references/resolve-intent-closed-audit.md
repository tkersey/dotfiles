# Auditing Intent-Closed `$resolve`

Native owner:

```text
review-compiler-audit --protocol mbk
adjudication-audit
skill-decision-audit
decision-capsule
```

Future fields/specification:

```text
SEQ_INTENT_CLOSED_RESOLVE_AUDIT_SPEC.md
```

Measure:

```text
AC seal/rebase
review modes and batches
whole-diff conformance violations
raw claims -> confirmed CEX -> novel classes
additional/duplicate witnesses
outside-horizon findings
kernel/realization invalidations
PHI strict-progress cycles
construct/law and proof/law coverage
terminal holdout
mutation while batch open
```

Do not count:

```text
raw `$resolve` mention
skill block
context persistence
generic review completion
```

as successful intent-closed operation.

Decision-capsule should normalize:

```text
AC seal/rebase
CEX admission/disposition
class merge/split
kernel acceptance
design selection
realization invalidation
return-to-kernel/contract
PHI gate
terminal closure
```
