# Minimum Behavioral Kernel Audit

When the installed `seq` supports it:

```bash
seq review-compiler-audit --protocol mbk ...
```

Audit:

- fixed campaign base;
- review-ready baseline;
- findings compressed into governing laws;
- kernel distinctions and quotient method;
- realization from campaign base;
- hard semantic-surface changes;
- proof-family compression;
- physical controller apply/commit/push;
- tuple vs terminal closure;
- unqualified completion claims.
