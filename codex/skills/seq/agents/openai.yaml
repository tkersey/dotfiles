interface:
  display_name: "seq"
  short_description: "Mine Codex sessions and memories with the Zig seq CLI"
  default_prompt: "Use $seq (Zig CLI) to analyze ~/.codex/sessions/ and ~/.codex/memories for usage, compliance, memory mining, and orchestration concurrency from spawn_agents_on_csv artifacts."
