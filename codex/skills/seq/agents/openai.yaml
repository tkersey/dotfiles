policy:
  allow_implicit_invocation: true
interface:
  display_name: "seq"
  short_description: "Mine sessions, decisions, tools, and memory"
  default_prompt: "Use $seq with the narrowest lifted command; prefer skill-decision-audit for skill influence, skill-evidence for presence, and generic query only when no specialized surface fits."
