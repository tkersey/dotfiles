interface:
  display_name: "seq"
  short_description: "Mine Codex sessions and memories with the Zig seq CLI"
  default_prompt: "Use $seq (Zig CLI) to inventory and route Codex memories in ~/.codex/memories, especially memory_summary.md, MEMORY.md, rollout_summaries/, and raw_memories.md, then trace memory-backed claims into ~/.codex/sessions when needed; hard gate: run opencode research only when the user request includes literal `opencode`."
