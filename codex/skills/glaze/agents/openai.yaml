policy:
  allow_implicit_invocation: true
interface:
  display_name: "glaze"
  short_description: "Challenge a frozen incumbent with one bounded reframe"
  default_prompt: "Use $glaze once on the bound decision surface, preserve its canonical one-line escalation verbatim, return `material-reframe`, `no-material-reframe`, or `blocked` with the material delta and falsifier, and leave selection, mutation, and closure to the owning workflow."
