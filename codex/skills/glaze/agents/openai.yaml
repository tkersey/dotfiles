policy:
  allow_implicit_invocation: true
interface:
  display_name: "glaze"
  short_description: "Challenge a frozen incumbent with one bounded reframe"
  default_prompt: "Use $glaze once to challenge a bound incumbent and search for a materially different frame, invariant, mechanism, artifact, or breakthrough candidate. Preserve the canonical one-line escalation verbatim."
