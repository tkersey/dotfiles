policy:
  allow_implicit_invocation: true
interface:
  display_name: "Optimize Skills"
  short_description: "Orchestrate seq, tune, and user-owned refine"
  default_prompt: "Use $opt to route evidence through $seq/$shadow, diagnosis through $tune, and all authorized skill-package optimization through the user-owned $refine skill."
