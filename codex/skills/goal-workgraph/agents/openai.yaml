policy:
  allow_implicit_invocation: true
interface:
  display_name: "Goal WorkGraph"
  short_description: "Project accepted work into bounded nodes"
  default_prompt: "Use $goal-workgraph to project this accepted run or review resolution into verifier-bound owner-level work nodes."
