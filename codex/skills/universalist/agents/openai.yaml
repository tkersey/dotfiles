interface:
  display_name: "Universalist"
  short_description: "Apply Universalist at every code boundary."
  brand_color: "#6D5EF6"
  default_prompt: "Use $universalist to inspect the code boundary this implementation or resolution creates, changes, preserves, repairs, or removes; choose the smallest honest artifact and law witness, including effective halos and locality preservation when local context is semantic."
policy:
  allow_implicit_invocation: true