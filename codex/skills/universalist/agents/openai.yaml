interface:
  display_name: "Universalist"
  short_description: "Recognize latent structure and nominate exact boundaries"
  brand_color: "#6D5EF6"
  default_prompt: "Use $universalist to identify the current boundary, semantic owner, required observations, ordinary candidate, law, and falsifier. Preserve an already exact boundary. Load latent-recognition, consequential-decision, advanced-mechanics, durability, or team references only when their evidence gates fire. In Actuating, nominate without selecting."
policy:
  allow_implicit_invocation: true
