interface:
  display_name: "mesh"
  short_description: "Plan-driven CSV-wave orchestration with coder/fixer/integrator gates"
  default_prompt: "Use $mesh to execute update_plan work in parallel CSV waves with triplet-first coder/fixer/integrator cohorts, safe degrade/restore fanout, integrator-only delivery, and strict worker output parsing."
