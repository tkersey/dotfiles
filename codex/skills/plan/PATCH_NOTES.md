# `$plan` 4.0 Patch Notes

The skill now compiles an observable execution policy rather than a speculative full task list.

```text
EPG-v1 -> EPS-v1 -> EPD-v1 -> one commitment horizon -> ETR-v1 -> next EPS-v1
```

Removed as readiness authority:

```text
Iteration: N
clean-round counters
self-reported rewrite ratios
mandatory innovation additions
self-attested convergence
```

Added:

```text
system-regime classification
belief/unknown/observation model
guarded actions with predictions
safety shield
lexicographic progress potential
terminal-state closure
atomic state transition tooling
```
