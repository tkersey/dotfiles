policy:
  allow_implicit_invocation: false
interface:
  display_name: "Plan"
  short_description: "Compile architecture-aware execution policies"
  default_prompt: "Use $plan to compile accepted intent into one source-bound, architecture-aware EPG-v1 policy that passes the execution-policy compiler."
