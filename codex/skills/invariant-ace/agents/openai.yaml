policy:
  allow_implicit_invocation: false
interface:
  display_name: "Invariant Ace"
  short_description: "Authority-gated invariant design with counterexamples, owner/scope, induction, boundary, witness parity, and verification gates"
  default_prompt: "Use $invariant-ace in Authority-Gated v1 mode for this task."
