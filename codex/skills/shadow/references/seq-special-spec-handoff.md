# Seq Special-Spec Handoff

When `$shadow` discovers a `$seq` CLI gap, produce a special-spec brief rather than changing `$seq`.

Required sections:

- need;
- observed gap;
- required behavior;
- acceptance criteria;
- validation commands.

Do not bundle `$seq` CLI changes into ordinary `$shadow`/`$tune` skill edits.
