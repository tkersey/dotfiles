policy:
  allow_implicit_invocation: false
interface:
  display_name: "Meta Orchestrator"
  short_description: "Coordinates exhaustive build, posture-aware review routing, and verification to a material fixed point"
  default_prompt: "Use the meta-orchestrator skill for this task."
