policy:
  allow_implicit_invocation: false
interface:
  display_name: "Meta Orchestrator"
  short_description: "Coordinates build, review, and verification across doctrine skills"
  default_prompt: "Use the meta-orchestrator skill for this task."
